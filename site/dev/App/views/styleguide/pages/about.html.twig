{% extends ajax ? "desktop/default/empty.html.twig" : "desktop/default/layout.html.twig" %}

{% block content %}

<div class="center">
	<h1>{{ content.title }}</h1>

	{{ content.text }}
</div>

{% endblock %}