{% extends ajax ? "desktop/default/empty.html.twig" : "desktop/default/layout.html.twig" %}

{% block content %}

<div class="center">
	<h1>{{ content.data.title }}</h1>
	<div>{{ content.data.content | raw }}</div>
</div>

{% endblock %}